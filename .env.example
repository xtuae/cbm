# ===============================================================================================
# ENVIRONMENT CONFIGURATION - CREDITS MARKETPLACE PLATFORM
# ===============================================================================================
# Copy this file to '.env' and fill in your actual values for the relevant Supabase project.
# Do NOT commit your .env file to version control - it contains sensitive credentials!
# ===============================================================================================

# ===============================================================================================
# 📱 FRONTEND ENVIRONMENT VARIABLES (VITE_*)
# ===============================================================================================
# These variables are EXPOSED TO BROWSERS and are NON-SENSITIVE.
# Available in frontend via import.meta.env.VITE_*

# Supabase Frontend Configuration (get from Supabase Project Settings → API)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ===============================================================================================
# 🔑 SERVER/SUPABASE CLI ENVIRONMENT VARIABLES
# ===============================================================================================
# These are SENSITIVE and NEVER exposed to browsers or committed.
# Used only for CLI operations and server-side authentication.

# Supabase Service Role Key (for CLI operations and Edge Functions)
# WARNING: Extremely privileged - use with maximum caution
# Get from: Supabase Dashboard → Settings → API → service_role secret
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase JWT Secret (for server-side JWT verification)
# Get from: Supabase Dashboard → Settings → API → JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ===============================================================================================
# ⚠️  IMPORTANT NOTES
# ===============================================================================================
# - NEVER commit real credentials to version control
# - Use different Supabase projects for dev/staging/prod environments
# - Local development uses SAME Supabase project URLs/keys as live projects
# - Service role keys should NEVER be used in frontend code
# - Rotate keys immediately if compromise is suspected
# - Store production secrets in secure hosting platforms (Vercel, etc.)
# ===============================================================================================
